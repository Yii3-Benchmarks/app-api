-- This file is generated by tools/generate-pgsql-dump.php.
-- Do not edit directly.

DROP TABLE IF EXISTS orders;
DROP TABLE IF EXISTS customers;

CREATE TABLE customers (
    id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    email VARCHAR(180) NOT NULL UNIQUE,
    full_name VARCHAR(120) NOT NULL,
    segment VARCHAR(16) NOT NULL,
    city VARCHAR(64) NOT NULL,
    created_at TIMESTAMP NOT NULL
);

CREATE TABLE orders (
    id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    customer_id BIGINT NOT NULL REFERENCES customers(id),
    reference VARCHAR(16) NOT NULL UNIQUE,
    status VARCHAR(16) NOT NULL,
    total_cents INTEGER NOT NULL,
    item_count SMALLINT NOT NULL,
    created_at TIMESTAMP NOT NULL
);

INSERT INTO customers (email, full_name, segment, city, created_at)
SELECT
    format('customer-%s@example.test', gs),
    (ARRAY[
        'Olivia', 'Liam', 'Emma', 'Noah', 'Amelia', 'Elijah', 'Sophia', 'Lucas', 'Mia', 'Ethan'
    ])[1 + (gs % 10)]
    || ' ' ||
    (ARRAY[
        'Smith', 'Johnson', 'Williams', 'Brown', 'Jones', 'Miller', 'Davis', 'Garcia', 'Wilson', 'Moore'
    ])[1 + ((gs / 10) % 10)],
    (ARRAY['bronze', 'silver', 'gold', 'platinum'])[1 + (gs % 4)],
    (ARRAY['Berlin', 'London', 'Madrid', 'Moscow', 'New York', 'Singapore'])[1 + (gs % 6)],
    TIMESTAMP '2023-01-01 00:00:00'
        + ((gs % 720) * INTERVAL '1 day')
        + (((gs * 13) % 1440) * INTERVAL '1 minute')
FROM generate_series(1, 20000) AS gs;

INSERT INTO orders (customer_id, reference, status, total_cents, item_count, created_at)
SELECT
    1 + ((gs - 1) % 20000),
    'ORD-' || lpad(gs::text, 8, '0'),
    (ARRAY['pending', 'paid', 'packed', 'shipped', 'delivered', 'returned'])[1 + (gs % 6)],
    500 + ((gs * 137) % 250000),
    1 + (gs % 8),
    TIMESTAMP '2024-01-01 00:00:00'
        + ((gs % 400) * INTERVAL '1 day')
        + (((gs * 17) % 1440) * INTERVAL '1 minute')
FROM generate_series(1, 200000) AS gs;

CREATE INDEX idx_orders_created_at ON orders (created_at DESC, id DESC);
CREATE INDEX idx_orders_customer_created_at ON orders (customer_id, created_at DESC, id DESC);
CREATE INDEX idx_customers_segment_city ON customers (segment, city);

ANALYZE customers;
ANALYZE orders;
